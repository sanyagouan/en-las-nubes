# Configuración por defecto para sites-enabled
# Este archivo es incluido automáticamente desde nginx.conf

server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    # Deshabilitar servidor por defecto para seguridad
    return 444;
}